//
//  TableViewCell.swift
//  SampleApp
//
//  Created by Ryota on 2020/12/15.
//

/*mport UIKit

class TableViewCell: UITableViewCell {
    
    @IBOutlet weak var titleLabel: UILabel!
    @IBOutlet weak var idLabel: UILabel!
    
    override func awakeFromNib() {
        super.awakeFromNib()
    }
    
    func bindData(article: Article) {
        titleLabel.text = article.title
        idLabel.text = article.userId
    }
}

class MyUIPaddingLabel: UILabel {
    var tagPadding:CGFloat = 10
    override func drawText(in rect: CGRect) {
        let insets = UIEdgeInsets(top: 0, left: tagPadding, bottom:0, right: tagPadding) //セルの左右に余白を空けて視認性を上げました。
        super.drawText(in: rect.inset(by: insets))
    }
}
*/
